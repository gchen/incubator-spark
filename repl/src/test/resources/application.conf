spark {
    master {
        host = localhost
        port = 7077
        webui.port = 8080
    }

    debugger {
        enabled = true
        checksum = true
        logPath = /tmp/event.log
    }
}
